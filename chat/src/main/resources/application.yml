spring:
  application:
    name: chat
  datasource:
    url: jdbc:sqlserver://snapseat.database.windows.net:1433;database=gautam-dev;encrypt=true;trustServerCertificate=false;hostNameInCertificate=*.database.windows.net;loginTimeout=30;
    username: CloudSA224af24a@snapseat
    password: Gautam@123
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.SQLServerDialect

message:
  service:
    url: http://localhost:8082   # URL where Message Service runs

server:
  port: 8081